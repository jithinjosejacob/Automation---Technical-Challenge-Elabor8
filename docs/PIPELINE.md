We use github actions to run the CI Pipeline to do quality checks on our api tests.

Inorder to run this tests after deployment, we just need to trigger the current pipeline after deployment is complete.
